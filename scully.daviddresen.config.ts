import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "daviddresen",
  outDir: './dist/static',
  routes: {
  },
  extraRoutes: ['/contact']
};
